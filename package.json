{
  "name": "cafeteria-root",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:all": "npm run lint --prefix backend",
    "test:all": "npm run test:cov --prefix backend && npm run test --prefix frontend"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  }
}
